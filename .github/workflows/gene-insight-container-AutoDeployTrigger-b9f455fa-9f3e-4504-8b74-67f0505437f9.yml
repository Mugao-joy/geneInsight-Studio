name: Trigger auto deployment for gene-insight-container

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/gene-insight-container-AutoDeployTrigger-b9f455fa-9f3e-4504-8b74-67f0505437f9.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.GENEINSIGHTCONTAINER_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.GENEINSIGHTCONTAINER_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.GENEINSIGHTCONTAINER_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.GENEINSIGHTCONTAINER_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.GENEINSIGHTCONTAINER_REGISTRY_PASSWORD }}
          containerAppName: gene-insight-container
          resourceGroup: registry-resource-group
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


